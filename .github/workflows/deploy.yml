name: CI/CD NexTraceWeb Frontend

on:
  push:
    branches: [ main ]

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
    - name: Install dependencies
      run: npm install
    - name: Run Cypress tests
      run: npx cypress run
    - name: Build frontend
      run: npm run build
    - name: Deploy placeholder
      run: echo "🚀 Deploy iniciado (configure destino)"
